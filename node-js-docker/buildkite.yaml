# This is an example on how to test a Node.js project using Buildkite and Docker. It uses the standard Node.js Docker image and Buildkite’s Docker-based Builds.

# For more information, see: https://github.com/buildkite/nodejs-docker-example

steps:
  - name: ":node:"
    command: npm test # suggested command
    plugins:
      - docker-compose#v3.5.0:
          run: app # you will need to define app within docker-compose.yaml in your repository
