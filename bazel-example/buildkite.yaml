# This is an example for how to use Bazel to build a C++ project.
# This assumes you have installed Bazel on your Buildkite agent.
# For more information, see: https://github.com/buildkite/bazel-example

steps:
  - label: ":bazel: Build"
    commands:
      - bazel build //main:hello-world
