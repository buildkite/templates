---
name: Python
description: Test a python project using Buildkite and pipenv
language: Python
---
# This installs your dependencies and runs the path specified in the run command, which you will need to include in your repository.
# This assumes your Buildkite Agent has Python and pipenv already installed.
# For more information, see https://github.com/buildkite/python-pipenv-example

steps:
  - label: ":python:"
    commands:
      - pipenv install --deploy --dev
      - pipenv run "Suggested path py.test "
