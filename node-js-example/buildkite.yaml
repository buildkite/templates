# This is an example of how to test a Node.js project using Buildkite.
# Note: this example assumes your Buildkite Agent machine has Node.js already installed.

# For more information, see: https://github.com/buildkite/nodejs-example

steps:
  - name: ":node:"
    command: "npm install && npm test"
