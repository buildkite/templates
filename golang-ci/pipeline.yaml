steps:
  - label: ":golang: Install"
    commands:
      - "go install"
    key: "deps"

  - label: ":golang: Build"
    commands:
      - "go build main.go"
    depends_on: ["deps"]

  - label: ":golang: Test"
    commands:
      - "go test ."
    depends_on: ["deps"]
